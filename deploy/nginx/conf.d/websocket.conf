# AI Image Generator — WebSocket 支持
#
# Next.js HMR 在开发模式下需要 WebSocket。
# 生产环境可以注释掉，保持兼容即可。

proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection "upgrade";
proxy_read_timeout 86400s;  # 长连接超时 24h
